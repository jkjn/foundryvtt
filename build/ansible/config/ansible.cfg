[defaults]
roles_path = ../roles
gather_subset = !all
nocows = 1
verbosity = 1
timeout = 30

host_key_checking = false
remote_tmp = /tmp

[ssh_connection]
ssh_args = -o ServerAliveInterval=60 -o ControlMaster=auto -o ControlPersist=30m
pipelining = true